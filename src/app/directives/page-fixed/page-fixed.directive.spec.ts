import { PageFixedDirective } from './page-fixed.directive';

describe('PageFixedDirective', () => {
  it('should create an instance', () => {
    const directive = new PageFixedDirective();
    expect(directive).toBeTruthy();
  });
});
