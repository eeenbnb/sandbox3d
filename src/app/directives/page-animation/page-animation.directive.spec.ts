import { PageAnimationDirective } from './page-animation.directive';

describe('PageAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new PageAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
